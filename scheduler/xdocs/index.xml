<?xml version="1.0"?>

<document>

  <properties>
    <title>Scheduler Component</title>
    <author email="epugh@upstate.com">Eric PUgh</author>
  </properties>

  <body>

  <section name="Overview">
    <p>
      This component provides a scheduler service.  It is based on the TurbineScheduler 
      provided with Turbine, but all older stuff has been removed.  Currently ONLY the
      non persistent Scheduler is done.  It loads scheduled jobs from the component config
      xml file.
    </p>    
  </section>
  
<section name="Configuration">

    <p>
      First, here is the role configuration for using the non persistent Scheduler.
    </p>

<source>
<![CDATA[
  <role
        name="org.apache.fulcrum.schedule.ScheduleService"
        shorthand="scheduler"
        default-class="org.apache.fulcrum.schedule.turbine.nonpersistent.NonPersistentSchedulerService"/>
]]>
</source>

  <p>
    Here is the configuration.  This is basically like a cron job.
  </p>
<source>

<![CDATA[
    <scheduler>
    	<NonPersistentSchedulerService>
	    	<jobs>
	    		<job>
	    			<classname>org.apache.fulcrum.schedule.turbine.SimpleJob</classname>
	    			<id>1</id>
	    			<second>10</second>
	    			<minute>-1</minute>
	    			<hour>-1</hour>
	    			<weekday>-1</weekday>
	    			<dayofmonth>-1</dayofmonth>
	    		</job>
	    	</jobs>
	    </NonPersistentSchedulerService>
    </scheduler>
]]>
</source>

  </section>

  <section name="Usage">
    <p>
    </p>
  </section>  

</body>
</document>
